#!/usr/bin/env bash
set -Eeuo pipefail

echo "deploy.exp now uses the guarded, environment-specific deployment workflow."
echo "SSH key authentication and HOSTINGER_SSH_HOST/HOSTINGER_SSH_USER are required."
exec "$(git rev-parse --show-toplevel)/scripts/deploy-hostinger.sh" "${1:-staging}"
